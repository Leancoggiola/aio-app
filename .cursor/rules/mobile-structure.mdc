---
description: Capas y features de apps/mobile (Expo Router)
globs: apps/mobile/**/*
alwaysApply: false
---

# Mobile — estructura

## Capas (`apps/mobile/`)

| Capa | Uso |
|------|-----|
| `app/` | Expo Router (rutas, tabs, login) |
| `src/features/` | Dominio (auth, home, media, profile) |
| `src/shared/api/` | `API_KEYS`, client Bearer |
| `src/core/auth/` | AuthProvider, SecureStore |
| `src/theme/` | Tamagui config |

## Reglas de imports

- `@/shared/api`, `@/core/auth`, `@/features/...`, `@/theme/...`
- **Prohibido:** feature A → feature B
- Contratos: `@omni/shared/*` (no duplicar schemas)

## UI

Tamagui (`tamagui`). No Mantine. No inventar paths `/api/` fuera de `API_KEYS`.

Referencia: [docs/mobile/tooling.md](../../docs/mobile/tooling.md) · skill `mobile-structure`.
