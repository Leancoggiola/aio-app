services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aio
      POSTGRES_PASSWORD: aio_dev
      POSTGRES_DB: aio_app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
