/** API / storage format — same convention as Mantine DatePicker presets. */
export const ISO_DATE_FORMAT = 'YYYY-MM-DD';

/** User-facing date format (matches DatePickerInput valueFormat). */
export const DISPLAY_DATE_FORMAT = 'DD/MM/YYYY';
